<h1>id: <%= @task.id %> のタスク編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '修正' %>
<% end %>

<p><%= link_to '一覧に戻る', tasks_path %></p>
